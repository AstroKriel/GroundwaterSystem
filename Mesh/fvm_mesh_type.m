classdef fvm_mesh_type
    % Author: Mitchell Johnson
    enumeration
        UNIFORM,
        % UNIFORM_with_global_noise,   % this is now unused.
        % UNIFORM_with_granular_noise, % this is now unused.
        GRADIENT,
        % GRADIENT_with_global_noise,  % this is now unused.
        % GRADIENT_with_granular_noise % this is now unused.
    end
end